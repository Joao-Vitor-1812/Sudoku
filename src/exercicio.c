#include <stdio.h>

bool verificacaoVazio(int sudoku[][], int linha, int coluna){
    if(sudoku[i][j] == 0){
        sudoku[i][j + 1];
        return true;
    }
}

void colocarNumero(int sudoku[][], int linha, int coluna){
    for(int i= 0; i <= 4;i++){
        for(int i= 0; i <= 4;i++){
            if(sudoku[i][j])
        }
    }
}

int main(){
    int linha = 4;
    int coluna = 4;
    int sudoku[linha][coluna] = 
    {0,2,4,0},
    {1,0,0,3},
    {4,0,0,2},
    {0,1,3,0};
}